"use client";

/**
 * Placeholder: calculator widget (e.g. premium / replacement value).
 * Implement when needed.
 */
export function CalculatorWidget() {
  return <div>CalculatorWidget (placeholder)</div>;
}
